apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: k8s-example
data:
  USE_VAULT: "true"
  VAULT_ADDR: http://k8s-example-vault.vault.svc.cluster.local:8200
  VAULT_ROLE: myapp
  VAULT_SECRET_PATH: secret/data/myapp
  